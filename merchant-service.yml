spring:
  application:
    name: merchant-service

  datasource:
    url: jdbc:mysql://localhost:3307/merchant_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
